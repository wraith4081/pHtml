<?php
class tag {
  // Properties
  public $tag;
  public $text;

  // functions
  function createvalue($tag, $text, $type) {
    var returnvalue = "<" . $tag . ">" . $text . "</" . $tag . ">";
    $type $returnvalue;
    var tagcount
    if ($tagcount) {
      $tagcount = $tagcount + 1;
    } else {
      $tagcount = 1;
     }
  }
}
    function count() {
      return $tagcount;
     }
$tag = new tag();
?>
